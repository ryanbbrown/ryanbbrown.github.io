# ryanbbrown.github.io

Personal website built with Hugo.

## Content Management

Website pages live in `content/` as regular Markdown files.

Edit these files in the repository with BB or another Markdown editor, then commit and push the changes through Git.

## Local Development

```bash
hugo server     # Run dev server at http://localhost:1313
```
